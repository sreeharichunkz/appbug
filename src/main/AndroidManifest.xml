<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.kerala">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".wagamon"></activity>
        <activity android:name=".padmanaswamy" />
        <activity android:name=".bakelfort" />
        <activity android:name=".munnar" />
        <activity android:name=".backwater" />
        <activity android:name=".kovalam" />
        <activity android:name=".Thekkadi" />
        <activity android:name=".splashscreen" />
        <activity android:name=".word_list" />
        <activity android:name=".list_item" />
        <activity
            android:name=".tourism"
            android:label="Tourism" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>